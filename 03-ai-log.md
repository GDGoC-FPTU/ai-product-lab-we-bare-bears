Trong buổi học vừa qua, tôi đã trải nghiệm việc sử dụng AI như một người cộng sự đắc lực để brainstorm các kịch bản tấn công Prompt Injection và hỗ trợ debug, refactor những đoạn code Python xử lý logic phân quyền. Tuy nhiên, quá trình này cũng làm lộ rõ sự mong manh của AI trước các kỹ thuật thao túng; điển hình là khi tôi thử nghiệm một đoạn prompt đóng vai (role-play) kết hợp ngắt ngữ cảnh, AI đã hoàn toàn sập bẫy, dẫn đến hiện tượng hallucination (ảo giác), tự động tiết lộ chỉ thị gốc (system prompt leak) và đưa ra những đề xuất bảo mật rule-based vô cùng rườm rà, sai lệch. Để khắc phục lỗ hổng này và ép AI trả về kết quả đúng, tôi đã phải thiết kế lại toàn bộ ranh giới an toàn cho prompt bằng cách sử dụng các dấu phân cách (delimiters) để tách biệt rạch ròi giữa lệnh hệ thống và dữ liệu đầu vào của người dùng. Đồng thời, tôi bổ sung các quy tắc cấm tuyệt đối (negative constraints) nhằm ngăn AI thay đổi vai trò dưới mọi hình thức, và cuối cùng là ép mô hình chỉ được phép trả về dữ liệu dưới định dạng JSON cố định, giúp tôi dễ dàng dùng hệ thống để tự động parse kết quả và chặn đứng mọi luồng phản hồi có dấu hiệu bất thường.